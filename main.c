#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
	
int combination();
int fac(int n);
int get_integer();

int get_integer();

{
	printf("정수를 입력하십시오 : ");
	int a;
	scanf("%d,&a");
	int input =(a); 
	return a;
}

int combination(int b, int c);
{
	int b,c;
	int result=fac(b)/(fac(c)*fac(b-c));
	return result;
	printf("%d",result); 
	
}

int fac(int n);
{
	int n;
	int res=1;
	int i;
	for (i=1; i<n; i++)
	   res=res*i;
	return res;
 } 
int j,k;
j=get_integer();
k=get_integer();

int y;
y=combination(j,k);







	return 0;
}

